# FR3 Hybrid Planning platform

Real Franka FR3 kinematics + MoveIt 2 Hybrid Planning + MuJoCo execution, built as the experimental platform for the "Predictive Physical Realizability" paper (`../predictive_realizability_paper_draft.md`, one level up — this `ros2_ws/` tree is `.gitignore`'d from that paper repo on purpose: it's engineering infrastructure, not paper content, so it has its own git history in *this* directory).

The paper's core claim is a certificate-driven, predictive local planner (**B3**) that catches a future torque violation before it happens, versus a reactive baseline (**B2**) that only checks the current instant. This platform ports both, plus the reduced-order Python reference's Level 0-4 response hierarchy, into real MoveIt 2 Hybrid Planning C++ plugins running against MuJoCo-simulated FR3 dynamics — not a toy model.

## Status: 
**Major correction (#33): the goal-execution-fragility "real physical/safety boundary" conclusion below is retracted** — the actual cause was an unused `<position>` actuator block in `fr3.xml` silently fighting the real effort controller, now removed. Fixed and independently re-verified live (qualitatively different, non-oscillating convergence), though full `task_success` was not reproduced with this session's own test harness — see #33 and "Next steps" #1 for the full, honest account before reading anything below about joint7/base-joint torque thresholds as physical fact.

Phases 0–3d, 4a, 4b done. Phase 4c: Exp2/Exp3 re-run on a deterministic replayed trajectory (fixes a real experimental-validity gap, see Phase 4c-fix below) — Exp2's finding holds up (**B3's certificate activates 4kg earlier than B2's reactive limit in the payload sweep** — see #32 for why "prediction gives a 4kg lead" overclaims this: B2 and B3 use different trigger thresholds, not just different lookahead). Exp3's originally-reported detection lead did NOT reproduce under the first pass of the fix, which led to root-causing a second, independent bug (local-replanning velocity/acceleration scaling silently ignored, see "Real bugs found and fixed" #12) — with BOTH bugs fixed, Exp3's ~0.36-0.38s detection lead reproduces robustly (latest, execution-start-anchored numbers: B3 detects `0.324s` after LOCAL EXECUTION BEGINS — not "after force onset," a labeling error corrected in #32; the actual force disturbance starts partway into that window, so detection is really `~0.06s` after the disturbance itself begins — `T_warning≈0.40-0.41s`; B2 never detects — see #24, #32). Exp4 (known contact transition) now has the same replay-determinism fix as Exp2/Exp3 (#19) but is still blocked on the goal-execution-fragility issue — root cause now fixed (#33), oscillation eliminated, but `task_success` still not reached on `"large"` (see "Known environmental gaps").

## Architecture

```
fr3_mujoco_bringup/     Bring-up: URDF/xacro, MoveIt config, MuJoCo model, 
                        launch files, controller config. No governor logic of its 
                        own — wires everything else together. Both the URDF and 
                        the MJCF carry a matching "payload" link/body (Phase 4b) 
                        at fr3_link8's offset, mass set at launch time from 
                        FR3_PAYLOAD_MASS_KG — see "Real bugs found and fixed" 
                        below for why BOTH models need it, not just the simulated one.
fr3_dynamics/           Shared KDL-based inverse dynamics (mass matrix,
                        Coriolis+gravity, KDL<->MoveIt joint index
                        mapping). Used by B2 and B3 so they share ONE
                        dynamics code path (paper's B2-vs-B3 fairness
                        principle) — this is load-bearing, not incidental.
                        Also has a Jacobian + FK solver and a shared
                        ForceSchedule struct (Phase 4c) for external
                        end-effector force handling (ramp or
                        position-triggered spring).
fr3_b2_local_planner/   B2: reactive baseline. Current-instant-only
                        torque-feasible qddot projection via OSQP.
fr3_b3_local_planner/   B3: predictive certificate + Level 0/1/2/4
                        response (see below). The bulk of the interesting
                        code lives here.
```

`fr3_b3_local_planner` internals:

```
torque_margin_certificate.{hpp,cpp}   certificate.py::m_phys, shared by
                                       both plugin targets.
route_retime_search.{hpp,cpp}         Level 1: retiming transform +
                                       bisection/dense-grid search.
reshape_qp.{hpp,cpp}                  Level 2: OSQP reshape QP, shared by
                                       both plugin targets.
via_point_trajectory.{hpp,cpp}        Level 3: two-segment quintic
                                       stop-and-go candidate route
                                       (q0->q_via->qf), horizon_trajectory_
                                       operator_plugin only.
horizon_trajectory_operator.cpp       TrajectoryOperatorInterface plugin:
                                       exposes a real receding horizon
                                       (stock SimpleSampler only exposes
                                       one waypoint) + owns the ROUTE-LEVEL
                                       Level 1/2/3 decision (once per new
                                       trajectory).
b3_constraint_solver.cpp              LocalConstraintSolverInterface
                                       plugin: certificate check + ONLINE
                                       Level 0/2/4 decision (every cycle).
```

Third-party trees (`franka_description`, `moveit_hybrid_planning`, `mujoco_ros2_control`, `mujoco_menagerie_sparse`) are `.gitignore`'d here — each is its own clone with its own upstream history. Two of them carry local-only patches (not pushed, no push access to upstream):
- `moveit_hybrid_planning`: `@loader_path` install-RPATH fix (see below).
- `mujoco_ros2_control`: real-time pacing patch (sleeps off sim-vs-wall-clock drift each outer-loop iteration).

## The Level 0–4 hierarchy (paper Sec. V-B, `code/local_planner.py`)

| Level | What | Where | Cadence |
|---|---|---|---|
| 0 | Pass nominal through | both plugins | — |
| 1 | Retime (slow down by λ∈[1,λ_max]) | `HorizonTrajectoryOperator` | once per new route |
| 2 | Reshape (QP over qddot) | both, route- and cycle-level | route: once/route; online: every cycle |
| 3 | Reroute (via-point candidate, caller-configured) | `HorizonTrajectoryOperator` | once per new route, only if 1 and 2 both fail |
| 4 | Sticky brake | `B3ConstraintSolver` | every cycle, once triggered, forever |

Ordering per route: **retime → reshape → reroute**, tried only if the previous one didn't fix things; reroute itself re-runs retime then reshape on the alternate candidate (sub-levels 0/1/2) before giving up. Online, per cycle: **reshape → brake**. This exactly mirrors `code/local_planner.py::plan_route` / `online_step`'s own cascade. Level 3's candidate is **caller-configured** (`b3.via_point_offset.*`, all-zero by default = no candidate = true no-op), not searched for or generated — matches the Python reference's own explicit scope ("certificate-guided selection among caller-supplied candidates, not a general replanner").

## Phase-by-phase history

- **Phase 0–1**: environment bring-up; real FR3 kinematics + MoveIt 2 planning + MuJoCo execution, no governor. Proved the base pipeline.
- **Phase 2**: B2 as a real `LocalConstraintSolverInterface` plugin (current-instant OSQP torque projection). Verified genuinely different behavior between within-limits and artificially-over-limit goals.
- **Phase 3a**: B3's certificate + Level 0/4 only. New `HorizonTrajectoryOperator` (needed because stock `SimpleSampler` only ever exposes one waypoint, not a horizon). `fr3_dynamics` extracted as a shared package so B2 and B3 don't duplicate dynamics math.
- **Phase 3b**: Level 1 (route-level retiming). New shared `torque_margin_certificate.{hpp,cpp}` (extracted `B3ConstraintSolver`'s margin math so the route-level search reuses it) + `route_retime_search.{hpp,cpp}`.
- **Phase 3c**: Level 2 (reshape), online + route-level. New `reshape_qp.{hpp,cpp}`, a much larger sparse OSQP QP than B2's single-step one (`3 × num_joints × n` variables).
- **Phase 3d**: Level 3 (reroute). New `via_point_trajectory.{hpp,cpp}` (two-segment quintic stop-and-go candidate, ported from `code/trajectory.py::ViaPointTrajectory`). Reused Level 1/2's own machinery unchanged to evaluate/fix the candidate — genuinely small new code surface for what the hierarchy diagram makes look like a big phase. Verified: the mechanism (candidate construction, margin evaluation, retime/reshape cascade on the alternate, correct fallthrough) works correctly in every trial; the *specific* via-point offsets tried (6 trials, several directions/magnitudes/joint combinations) consistently made a wrist-joint-tight test scenario worse, not better — the stop-and-go candidate's own extra peak-acceleration demand outweighed any gravity-loading benefit found, and this held even after retiming the candidate up to `lam_max`. See `fr3_b3_local_planner/config/b3_params_reroute_test.yaml`'s header for the full finding.
- **Phase 4a**: observability + metrics harness, the first slice of "port the experiment suite" (a large, multi-experiment body of work in the Python reference — split the same way Phase 3 was split into 3a–3d). Added `/diagnostics` (`diagnostic_msgs/DiagnosticArray`, no new interface package needed) published every cycle by both `B2ConstraintSolver` and `B3ConstraintSolver` — the per-cycle state that had no log line before (Level 0 pass-through is silent). New `scripts/run_experiment.py` (formalizes the manual pkill → fresh domain ID → launch → poll-ready → send-goal dance every prior phase did by hand into a reusable harness that also drives `ros2 bag record`) and `scripts/compute_metrics.py` (reads the bag via `rosbag2_py`, computes a deliberately-scoped subset of `code/metrics.py::RunMetrics`'s fields). Verified: B1 (stock)/B2/B3 all succeed on the standard within-limits goal with near-identical final error and zero interventions for B2/B3 — the direct analog of the Python reference's own Exp 1 "no regression" check. Along the way, found the real controller has a genuine steady-state joint-space tracking residual (~0.045–0.05 rad L2, confirmed stable across a 1s settle window, not a transient) that the `HybridPlanner` action's own "SUCCESS" doesn't reflect — `compute_metrics.py`'s success tolerance is set from this measured floor, not an idealized number.
- **Phase 4b**: payload sweep (Exp 2 analog). New end-effector `"payload"` body (MJCF, `fr3.xml`) + matching `"payload"` link/`payload_joint` fixed joint (URDF, `fr3_mujoco.urdf.xacro`), both at `fr3_link8`'s pose, mass threaded from one new `FR3_PAYLOAD_MASS_KG` env var into both models from the same launch-time value (see "Real bugs found and fixed" for why both are required, not just the simulated one). `run_experiment.py`/`compute_metrics.py` refactored into reusable `run_one()`/`compute()` library functions (still work as before from the CLI) plus a `"large"` goal choice, so the new `scripts/exp2_payload_sweep.py` can call them directly across many runs instead of shelling out per-cell. Verified end-to-end: **B3's certificate first triggers an online intervention at 6kg payload, 4kg before B2's own self-reported intervention at 10kg** — a genuine, monotonic "certificate lead" (`min_margin` trends 9.65 → 5.81 → 3.21 → 1.95 → 1.13 → -12.4 → -24.2 N·m as payload rises from 0 to 10kg). B1 could not be used as a comparison baseline on this goal at all — see "Known environmental gaps."
- **Phase 4c**: external end-effector force injection (Exp 3/4 analogs). `fr3_dynamics` gained a Jacobian (`computeJacobian`), an FK solver (`computeTipPosition`), and a shared `ForceSchedule` struct (`force_schedule.hpp`, ramp or position-triggered spring), threaded through every B2/B3 call site (`b2_constraint_solver.cpp`'s reactive check; `torque_margin_certificate.cpp`/`reshape_qp.cpp`/`route_retime_search.cpp`'s online AND route-level paths, gated by a new `b3.force_known_at_plan_time` flag that is the entire Exp3-vs-Exp4 code-path difference) — matches `dynamics.py::Arm.required_torque`'s own `tau - J(q)^T@F_ext` mechanism line for line. MuJoCo injection lives in `mujoco_ros2_control.cpp`'s `update()` loop, triggered by a shared one-shot `/fr3_force_injection/start` topic (`run_experiment.py`'s `run_one()` publishes it at goal-*acceptance* time, not launch time — see "Real bugs found and fixed"). **Exp3 (unanticipated ramp disturbance, `force_known_at_plan_time=false`)**: first pass done, with an apparently-positive result (B3 detects ~0.36-0.38s before ground truth, B2 never detects) that Phase 4c-fix below later found did not reproduce once the experiment's own methodology was fixed. **Exp4 (known-in-advance position-triggered contact, `force_known_at_plan_time=true`)**: blocked — see "Known environmental gaps" for the goal-execution-fragility platform issue found while designing its goal, not a Phase 4c bug.
- **Phase 4c-fix**: fixed a real experimental-validity gap an independent external review of `scripts/` found (Phase 4c's own item 10 below) — `exp2_payload_sweep.py`/`exp3_interaction_force.py` each triggered a *fresh*, randomized/unseeded OMPL plan per cell/baseline, so nothing guaranteed the paper's own "identical geometric trajectory, geometric path and time law held fixed" framing. New `fr3_replay_global_planner` package: a `GlobalPlannerInterface` plugin (`ReplayGlobalPlanner`) that deserializes and replays one captured real `moveit_msgs/MotionPlanResponse` verbatim instead of calling OMPL — selected via `FR3_GLOBAL_PLANNER_YAML`/`FR3_REPLAY_TRAJECTORY_PATH` env vars in all three launch files (both default to real, unmodified OMPL planning when unset — a true no-op, re-verified: default `small`-goal B3 run still reproduces `min_margin=9.6489` exactly). New `scripts/capture_trajectory.py` plans once via B1's own stock launch file and saves the resulting `/global_trajectory` (added to `run_one()`'s bag topics) as raw serialized bytes. **Verified deterministic**: two independent fresh replay launches of the same captured file produced byte-identical `/global_trajectory` messages and final joint positions matching to ~3e-7 rad (pure sim/control-loop noise, not planner randomness). `exp2_payload_sweep.py`/`exp3_interaction_force.py` now capture one trajectory at the start of `main()` and replay it for every cell. **Re-running both with the fix**: Exp2's finding held up essentially unchanged (`min_margin` trend 9.65 → 5.79 → 3.21 → 1.91 → 1.10 → -0.10 → -2.54 N·m over the same `[0,3,5,6,7,8,10]`kg sweep; B3 still triggers first at 6kg, B2 at 10kg, same 4kg certificate lead — though the *magnitude* at 8/10kg is now much smaller than the earlier random-OMPL run's -12.4/-24.2, since it reflects one specific fixed trajectory rather than an ensemble of different random ones). **Exp3's finding did NOT hold up on the first pass**: under the fixed, reproducible methodology (confirmed via a same-capture rerun matching the earlier run's margin trace to 5-6 significant figures), B3's online solve loop stopped at `m_phys≈2.05` N·m — still positive — just before the margin's own linear decline would cross zero (projected ≈0.09-0.1s later). Neither B2 nor B3 detected the violation in this run.
- **Phase 4c-fix, root cause + second fix**: root-caused why that window closed where it did. `LocalPlannerComponent::executeIteration()` (`moveit_hybrid_planning`'s own source) checks `getTrajectoryProgress() > 0.995` *before* every `solve()` call; B3's own progress flag is `current_duration_ >= total_duration`, a hard binary switch — the instant it flips, the component calls `succeed()`+`reset()`+`timer_->cancel()` immediately, stopping all further `solve()`/diagnostics with **no grace period** (an earlier version of this entry claimed a "~0.3s hold-at-terminal-state extension" here — that was wrong; the "hold at terminal state" behavior only fills unreached horizon steps *within* a still-active cycle, it does not extend the loop's own lifetime). So the window is bounded almost entirely by the route's own `total_duration`. Checking why THAT was so short (~0.33s) found a second, independent, pre-existing bug: both `HorizonTrajectoryOperator::addTrajectorySegment` (B3) and stock `SimpleSampler::addTrajectorySegment` (B1/B2) call `time_parametrization_.computeTimeStamps(*reference_trajectory_)` with no scaling arguments — `TimeOptimalTrajectoryGeneration::computeTimeStamps()` defaults both `max_velocity_scaling_factor`/`max_acceleration_scaling_factor` to 1.0 when omitted, and `moveit_msgs/MotionPlanResponse` (all `LocalPlannerComponent` ever sees, via its own `global_solution_subscriber_`) carries no such fields at all — so the original `MotionPlanRequest`'s own scaling was never reachable at local re-time, for ANY baseline, on ANY goal below 1.0x. Confirmed live: `"small"` (vel_scale=0.5) and `"small_slow"` (vel_scale=0.15) produced nearly identical real route durations (0.339s vs 0.325s) — `"small_slow"`'s entire "slowed down so the force ramp unfolds during real execution" premise had never actually held. Fixed by adding new `local_velocity_scaling`/`local_acceleration_scaling` node params (default 1.0, a true no-op) to both plugins, threaded into `computeTimeStamps()`, set via new `FR3_LOCAL_VEL_SCALE`/`FR3_LOCAL_ACCEL_SCALE` env vars in all three launch files. Re-verified as a true no-op by default (`min_margin=9.6489` on `small`, unchanged). The response to these new params turned out itself to be a genuinely odd, disclosed finding — see "Known environmental gaps" — but with a value from that same neutral sweep (0.02, found before ever checking detection outcomes), **Exp3's original result reproduces robustly**: B3 detects at 0.360s after force onset (T_warning = 0.7428 − 0.360 = 0.383s), confirmed via an independent same-capture rerun matching to the same 0.360s; B2 never detects. This is now real, deterministic, and re-verified twice — not the boundary-fragile artifact either the very first (pre-any-fix) or the intermediate (trajectory-only-fixed) version of this result was.

## Real bugs found and fixed along the way (worth knowing before you touch this code)

1. **macOS `@rpath` dlopen failure under `ros2 launch`** (Phase 3a). Pluginlib's `dlopen()` inside the running `component_container_mt` process couldn't resolve sibling-package `.dylib`s via `DYLD_LIBRARY_PATH`, even with a correctly-sourced overlay — affected `fr3_dynamics` AND an unrelated, pre-existing MoveIt plugin (`ReplanInvalidatedTrajectory` → `libsingle_plan_execution_plugin.dylib`, same directory). Root cause never fully nailed down (likely a `ros2 launch` composable-node-loading quirk specific to this RoboStack/macOS setup); fixed environment- independently with an explicit `@loader_path`-based `CMAKE_INSTALL_RPATH` in every affected `CMakeLists.txt`. **If you add a new package that depends on another package's shared library, you need this too** — copy the pattern from any existing `CMakeLists.txt` here (search for `CMAKE_INSTALL_RPATH`).
2. **Duplicate `declare_parameter` across plugins sharing a node.** Both `HorizonTrajectoryOperator` and `B3ConstraintSolver` load overlapping `b3.*` params into the *same* node (MoveIt loads both plugins into `local_planner_component`). Whichever initializes first (currently `HorizonTrajectoryOperator`) must declare; the other must read via `has_parameter`/`get_parameter` instead of a second `declare_parameter`, or it throws `ParameterAlreadyDeclaredException`. **Every new shared `b3.*` param needs this guard on both sides** — grep for `has_parameter("b3.dt")` for the pattern.
3. **`getStateAtDurationFromStart` writes into an already-allocated `RobotStatePtr`**, it does not allocate one. Passing a default-null pointer segfaults inside `RobotState::interpolate` (found via a macOS crash report, not guesswork).
4. **`HorizonTrajectoryOperator::addTrajectorySegment` must call `time_parametrization_.computeTimeStamps`** (like stock `SimpleSampler` does) — without it the buffered trajectory has no real duration and the local planner declares "done" almost instantly without the robot moving.
5. **The reshape QP's hard constraint only targets `|tau| ≤ tau_max - delta_tau`, not `m_safe`** (true of the Python reference too, not introduced here). Reshape reliably improves a genuine hard-constraint violation but won't always fully restore the separate `m_safe` safety buffer in one shot — this is expected, not a bug; Level 4 is the correct, always-available fallback when it doesn't.
6. **B2/B3's certificate is entirely blind to a payload added only to the MJCF** (Phase 4b). `fr3_dynamics` builds its KDL chain from the URDF (`kdl_parser`), completely independent of MuJoCo's own MJCF — adding a payload body to `fr3.xml` alone changes only the *simulated* physics, not what the certificate computes at all (confirmed via `B3_DEBUG_HORIZON`: byte-identical margins at 5kg vs. 0kg payload before the fix). Fixed by also adding a matching `"payload"` link to the URDF (`fr3_mujoco.urdf.xacro`, child of `fr3_link8` via a fixed joint) and switching B2/B3's `tip_link` config from `"fr3_link8"` to `"payload"` in all 8 relevant config YAMLs, so the mass genuinely enters the KDL chain. Re-verified after the fix: margin at step0 dropped from 9.92 (0kg) to 3.13 (5kg), confirming the certificate now sees it. **If you ever add another MJCF-only body expecting the certificate to react to it, it won't — the URDF is the only thing `fr3_dynamics` ever reads.**
7. **A leaked-daemon DDS hang during long sequential runs** (Phase 4b). `ros2 control load_controller` (used in every launch file here) auto-spawns a persistent per-`ROS_DOMAIN_ID` `ros2cli.daemon.daemonize` background process holding its own live rclpy node/DDS participant open for up to 2 hours of inactivity. Since `run_one()` picks a fresh random domain ID on every call, a long sequence of runs (e.g. a payload sweep) leaks one daemon per call; `pkill_stragglers()`'s pattern never matched them. Confirmed live: 25+ accumulated over a ~50-minute sweep and wedged the DDS transport solid (every UDP write failing, one cell hanging indefinitely with no error at all — it never even reached starting that cell's `ros2 launch`). Fixed by adding `daemonize` to `run_experiment.py`'s `PKILL_PATTERN`, so every `run_one()` call reaps all accumulated stragglers, not just its own. **Any new script that calls `run_one()` in a loop relies on this — don't bypass `pkill_stragglers()`.**
8. **`mujoco_ros2_control`'s own ROS subscriptions were never dispatched at all** (Phase 4c). `MujocoRos2Control::init()` only ever added `controller_manager_` to `cm_executor_` (its own `MultiThreadedExecutor`) — `node_` itself, the node any NEW subscription (e.g. the Phase 4c force-injection start signal) gets created on, was never spun by anything. Confirmed live: the callback simply never fired, silently leaving the SIMULATED force never applied even though B2/B3's own certificate (a separate process, correctly spun by ITS OWN container) reacted as if it had — a fully believable but wrong result if not caught. Fixed with one line, `cm_executor_->add_node(node_);`, reusing the already-running background executor thread rather than adding a new spin loop. **Any new subscription/timer added to `mujoco_ros2_control`'s own top-level node needs this — it is not spun by default.**
9. **The force-injection start trigger fired before `send_goal()`, not at goal acceptance** (Phase 4c). Publishing it right after `wait_ready()` (matching the original design) left several seconds of unpredictable global+local planning overhead for a multi-second force ramp to complete during, before any real execution ever began — confirmed live: a 0.5s ramp was already fully held by the time a single online `solve()` cycle ran. Fixed by adding an `on_accepted` hook to `send_goal()` that fires the trigger the moment the `HybridPlanner` action server accepts the goal (much closer to real execution start) — reusing `send_goal()`'s own already-active `rclpy` node rather than creating a second one (a nested `rclpy.init()` while already spinning is not safe).
10. **Three metrics/harness robustness bugs found by an independent external review of `scripts/` (Phase 4c), all confirmed real by re-reading the actual code before fixing, all fixed and re-verified against the established baseline** (`min_margin=9.6489` on the small goal, unchanged): (a) `compute_metrics.py` silently dropped any joint missing from the final `/joint_states` sample out of the L2 error sum instead of failing loudly — understates error, could produce a false `task_success`; now raises `RuntimeError` listing the missing joints. (b) `compute_metrics.py` assumed the *first* `/diagnostics` message's *first* status entry was the controller's own — fragile if any other diagnostic publisher ever appeared on the topic first; now explicitly searches every message for `"b2_constraint_solver"`/`"b3_constraint_solver"` by name. (c) `run_experiment.py`'s `run_one()` had no `try`/`finally` around the launch+bag+goal sequence — an exception inside `send_goal()` left `bag_proc`/`launch_proc` running until a *later* call's own `pkill_stragglers()` happened to clean them up; now wrapped so teardown always runs. **The same review also raised a deeper experimental-validity concern, since fixed — see item 11 below**: `exp2_payload_sweep.py` and `exp3_interaction_force.py` each independently triggered fresh (randomized, unseeded) OMPL planning per cell/baseline, so the paper's own "identical geometric trajectory, only X varies" framing for Exp2/Exp3 was not actually guaranteed by the code.
11. **Trajectory nondeterminism across `exp2_payload_sweep.py`/`exp3_interaction_force.py` cells** (Phase 4c-fix, the concern item 10 flagged but didn't yet act on). Fixed with a new `fr3_replay_global_planner/ReplayGlobalPlanner` plugin (a `GlobalPlannerInterface` implementation that deserializes and replays one captured real `moveit_msgs/MotionPlanResponse` instead of calling OMPL) plus `scripts/capture_trajectory.py` (captures that one real plan via B1's own stock launch file). Verified deterministic (two independent replay launches of the same capture: byte-identical `/global_trajectory`, final joint positions matching to ~3e-7 rad) and verified as a true no-op on the default (non-replay) path (`min_margin=9.6489` on the small goal, unchanged). **Any new sweep script that compares baselines against each other must use this same capture+replay pattern, or it inherits the same nondeterminism gap.**
12. **Local re-planning silently ignored the goal's own `max_velocity_scaling_factor`/`max_acceleration_scaling_factor`** (found while investigating why fix #11's re-run of Exp3 stopped reproducing its earlier "~0.36-0.38s lead" result — see "Phase 4c-fix, root cause + second fix" in the phase history above for the full derivation). Both stock `SimpleSampler` (B1/B2) and `HorizonTrajectoryOperator` (B3) called `computeTimeStamps()` with no scaling arguments, defaulting to full URDF joint-limit speed regardless of what the goal requested — `"small_slow"` (vel_scale=0.15) executed at essentially the same real pace as `"small"` (vel_scale=0.5), for every baseline, since Phase 3a. Fixed with new `local_velocity_scaling`/`local_acceleration_scaling` node params (default 1.0, a true no-op) on both plugins, set via `FR3_LOCAL_VEL_SCALE`/`FR3_LOCAL_ACCEL_SCALE`. With this AND fix #11 both applied, Exp3's original result reproduces robustly (B3 detects 0.360s after force onset, T_warning=0.383s, confirmed via an independent rerun; B2 never detects). **Any goal relying on a sub-1.0 `vel_scale`/`accel_scale` to genuinely slow down real execution (not just the OMPL search) needs these two env vars set explicitly — the goal's own request-level scaling alone still does nothing at the local-replanning level.**
13. **`run_experiment.py`'s `PKILL_PATTERN` was broader than it needed to be** (one of the external review's Medium-priority findings, acted on later). `pkill -9 -f` matches ANY process on the machine whose command line contains the substring, not just this project's own — confirmed live via `ps aux` during an active launch that `component_container_mt`/`robot_state_publisher` run from fully generic shared conda-env binary paths with no project-specific args, so several of the original terms (`"component_container"`, bare `"ros2 launch"`) could in principle kill an unrelated ROS 2 project's own same-named processes if one happened to be running at the same time. Fixed two ways: (a) `run_one()`'s own `launch_proc` now starts in its own process group (`start_new_session=True`) and is torn down precisely via `os.killpg` (new `kill_launch_tree()`) instead of relying on pattern matching at all for the normal (non-crashed) path; (b) `PKILL_PATTERN` itself — now only a fallback for orphans left by a run that died before its own Python process could call `kill_launch_tree()` — narrowed to `"ros2 launch fr3_mujoco_bringup"` (not bare `"ros2 launch"`) and `"hybrid_planning_container"` (not bare `"component_container"`/`"hybrid_planning"`), both confirmed present in this project's own real process argv with zero coverage loss. **Residual, disclosed risk**: `"robot_state_publisher"`/`"ros2_control_node"` could not be narrowed the same way (no project-specific marker in their own argv, no `name=` override in any launch file here) — still a real, if now much smaller, chance of collision with an unrelated simultaneous ROS 2 project. Re-verified live: 3 sequential `run_one()` calls left zero leftover project processes and zero leftover `ros2cli` daemons, `min_margin=9.6489` on `small` unchanged.
14. **`compute_metrics.py`'s `online_intervention_count` counted individual `/diagnostics` SAMPLES, not distinct intervention EPISODES** (another of the external review's Medium-priority findings). B2/B3 publish `/diagnostics` every control cycle they're active, so one sustained intervention spanning many cycles inflated the count far past what "how many times did it intervene" actually means — confirmed live on an existing `B3_10.0`/`B2_10.0` bag from the Exp2 sweep: the old metric would have reported **2301** and **4603** "interventions" respectively for what is, in both cases, **one** contiguous intervening episode. Fixed by splitting the field into `online_intervention_sample_count` (the old, cycle-level count, kept for anyone who wants duration) and a new `online_intervention_episode_count` (increments only on the not-intervening→intervening transition, sorted explicitly by timestamp first since episode counting is order-dependent, unlike the sample count). `exp2_payload_sweep.py`/`exp4_contact_transition.py` updated to use the episode count wherever the review's concern applies — `exp2_payload_sweep.py`'s own numbers were unaffected either way (it only ever used the field as a boolean truthy check, and 0-vs-nonzero is identical under either count), but `exp4_contact_transition.py` was printing the raw count directly, which would have been directly misleading once Exp4 is unblocked. Re-verified: full field rename plumbed through cleanly end-to-end (`exp2_payload_sweep.py 0 10` smoke test), no crashes, boolean flags unchanged.
15. **`compute_metrics.py`'s `task_success` was decided from a SINGLE final `/joint_states` sample** (another of the external review's Medium-priority findings). That one instant happening to be within tolerance doesn't verify the arm actually SETTLED there, only that it wasn't caught mid-transient at that exact moment — a real methodological gap even though empirical checks (three real scenario types: plain success, an ongoing-force-disturbance success, and a "stuck" large-payload failure) all showed the terminal window already extremely stable in practice (error unchanged to the 5th decimal for the whole window). Fixed with a new `terminal_window_max_error_rad` field: the WORST error across the last `SETTLE_WINDOW_S=0.3`s of real time (a fraction of `run_one()`'s own established 1.0s post-completion settle sleep, still inside the settled region even for `"small_slow"`'s own ~0.3-0.6s route), with `task_success` now deciding off that instead of the single-sample `final_pos_error_rad` (kept unchanged, still logged, for continuity with every prior established number). Re-verified on the same three real bags used to characterize the fix: `terminal_window_max_error_rad` exactly equals `final_pos_error_rad` in every case, so `task_success` is byte-for-byte unchanged everywhere it's currently used — a pure robustness improvement, not a behavior change. No call-site updates needed anywhere (`task_success`'s own field name/type didn't change).
16. **`duration_s` blended planning latency, real execution, and the deliberate settle tail into one number** (another of the external review's Medium-priority findings). Couldn't answer "was this run slow because of planning overhead or because execution itself took a long time." `moveit_msgs/MotionPlanResponse`'s own self-reported `planning_time` field would be the clean answer, but it's NOT populated by this platform's pipeline — confirmed live across 5 real bags (both real-OMPL and replay-based runs), always exactly `0.0`. Derived from bag timestamps instead: new `planning_latency_s` (bag start to `/global_trajectory`'s own receive time — works identically whether that trajectory came from a fresh OMPL plan or `fr3_replay_global_planner`'s replay) and `execution_duration_s` (that receive time to the LAST sample the joint-space error was still above `POS_TOL_RAD`, i.e. time to first sustained convergence, excluding the settle tail). Both `None` if `/global_trajectory` wasn't recorded. `duration_s` itself is unchanged, now documented as the full span including both. **Real, unplanned discovery made while verifying this fix, investigated the same session**: `execution_duration_s` on the `"large"` goal at 0kg payload (`/tmp/exp2_sweep/B3_0.0`, this session's own latest Exp2 rerun) came out as 46.0 of the run's 46.5s total — checked directly against raw joint_states and confirmed NOT a metric bug: the error genuinely never drops meaningfully below its own initial ~1.58 rad. Directly contradicted this README's own existing claim elsewhere ("B2 and B3 both execute this exact goal fine at payload=0"). Investigated live: a per-joint breakdown showed ALL SEVEN joints stalled at 1-7% of their own commanded displacement (not one joint blocking the rest) — e.g. joint6 needed 1.4−0.544=0.856 rad, moved only 0.058 rad in 60s. Re-ran at `FR3_LOCAL_VEL_SCALE=FR3_LOCAL_ACCEL_SCALE=0.1` (10x slower local re-time, using this session's own new mechanism, fix #12 above) — final error was statistically unchanged (1.511 vs. 1.505-1.579 rad across every earlier run), ruling out "route too aggressive for real dynamics to track" as the cause. **Conclusion: this is the SAME already-diagnosed "goal-execution-fragility" issue** (see "Known environmental gaps" below), not a new bug — every one of `"large"`'s own per-joint displacements from `"small"`'s own baseline (0.38-0.84 rad) is far past the ~0.13-0.15 rad threshold that issue already established as the trigger, and this session's own scale-sweep re-confirms (at an even lower 0.1x) that issue's own prior finding that scale isn't the variable at play. New evidence from this: B3 is ALSO affected, not just B1/B2, and the failure is uniform across all seven joints simultaneously (not isolated to whichever joint moves most) — folded into that issue's own writeup rather than tracked separately.
17. **No validation that B3's PREDICTED margin actually matches what's later physically OBSERVED** (the external review's "P2" finding, the last of its open items). Every prior check only verified the certificate fires ahead of the nominal-route crossing time — never that a specific future-margin prediction was itself accurate. `B3ConstraintSolver` now also computes and publishes `m_phys_observed`: the SAME certificate formula evaluated at the REAL measured robot state (position + velocity from `current_state`) instead of the reference horizon, borrowing the reference trajectory's own commanded acceleration for that instant rather than a noisy finite-difference estimate — this isolates what real tracking error does to the certificate's own claim. Deliberately NOT implemented as re-evaluating the same reference trajectory from a later cycle instead: `reference_trajectory_` doesn't change cycle to cycle absent a route-level Level 1/2/3 event, so that comparison would just reproduce the same deterministic number and validate nothing. New `scripts/validate_prediction.py` does the actual validation offline: for each cycle's own prediction (`m_phys` at time `t`, claiming the worst margin over the next `binding_step*b3.dt` seconds), it finds the closest LATER cycle's own `m_phys_observed` at that same absolute future time and reports the prediction error. Live-verified on a fresh force-disturbance run: 3 genuine future predictions (`binding_step=14`, 0.28s horizon) all landed close to what was later observed (errors of 0.17-0.23 N·m on an ~8 N·m margin, ~2-3% relative) — a real, positive result, not a forced one. No regression: `min_margin`/`m_phys` themselves are unchanged (`min_margin=9.6489` on `small`, exact match) since `m_phys_observed` is purely additive. **Precision note added later (third external review pass, re-verified directly against the C++ source)**: `m_phys_observed`'s own force input is the SAME commanded/modeled force used for `m_phys`'s own prediction, not an independently measured one — `observed_state` IS a genuine copy of the real measured robot state (confirmed by re-reading `b3_constraint_solver.cpp` line by line), only the acceleration and force model are shared. So this validates prediction of future actuator margin under MEASURED ROBOT-STATE EVOLUTION specifically, not a complete independent validation of the force model — the precise claim, now reflected in both the C++ and Python comments.
18. **`exp3_interaction_force.py`'s `first_detection_time` scanned `/diagnostics` in bag-read order, not timestamp order** (found while independently verifying a pasted, fabricated-looking "review" — its two concrete, checkable claims turned out to describe already-fixed code or were directly disproven by reading the real C++ log-generation source, but this one small inconsistency was real). `read_bag()`'s own read order isn't guaranteed to match timestamp order; "first detection" only means anything in time order. Fixed by sorting explicitly, matching `compute_metrics.py`'s own established `joint_states`/`diag_msgs` sorting precedent elsewhere in this codebase. Low practical risk in the first place (rosbag2 sqlite reads are normally already time-ordered) — re-verified on a real force-disturbance bag: `t_detect=0.360` unchanged, exact match to the previously-established value.
19. **`exp4_contact_transition.py` never got the trajectory-determinism fix Exp2/Exp3 got** (from a SECOND, genuine external review pass — its claims independently verified against real code before acting on any of them, unlike the fabricated one in #18). It still called `run_one()` independently per baseline with no `capture_trajectory`/`FR3_GLOBAL_PLANNER_YAML`/`FR3_REPLAY_TRAJECTORY_PATH` wiring at all — confirmed directly (`grep` found zero references) — so B1/B2/B3 could each get their own independently randomized OMPL plan, the exact gap already fixed elsewhere. Fixed by mirroring `exp3_interaction_force.py`'s own exact pattern (capture once at the top of `main()`, replay for every cell). Exp4 itself is still blocked/unusable (see "Known environmental gaps"), so this doesn't unblock any new result today, but the fix is now in place for whenever it is. Live-verified: capture succeeds for `"medium_slow"`, and a full `run_cell("B2", ...)` smoke test runs end-to-end with the replay env without crashing (`task_success=False`, consistent with the already-known, disclosed fragility issue -- not a new problem).
20. **`validate_prediction.py` only reported a per-prediction table + ad-hoc mean/max error**, not the RMSE/MAE the same review suggested for treating this as a real experiment result. Added `compute_summary()` (RMSE, MAE, mean error, max abs error, N). Re-verified on the same force-disturbance bag used to build this script: RMSE=0.1923 N·m, MAE=0.1897 N·m — matches hand-calculated values from the underlying per-prediction errors exactly.
21. **`planning_latency_s` measured from bag START, not the actual goal request** — silently included `run_one()`'s own ~0.5s pre-goal recorder-startup buffer, which the review correctly flagged as not true planning latency despite the honest disclosure comment already there. Fixed with a new one-shot `/fr3_experiment/goal_sent` marker (same `transient_local` pattern as the existing `/fr3_force_injection/start`), published by `send_goal()` the instant it actually sends the goal; `planning_latency_s` now measures from that marker's own bag timestamp to `/global_trajectory`'s receipt. Falls back to bag-start for older bags recorded before this marker existed (an unavoidable, disclosed limitation of reading old data, not of the new code). Live-verified: `planning_latency_s` dropped from ~0.42-0.5s (the old, buffer-inclusive number) to 0.021s on a fresh `small`-goal run — a genuinely sensible number for a trivial 2-waypoint solve. No regression (`min_margin=9.6490` unchanged).
22. **`ground_truth.py`/`ground_truth_failure_time` overclaimed what the number means** — it's B3's OWN certificate model, re-evaluated offline on the unmodified nominal route, not independent ground truth (already disclosed in this README as a "model-consistent reference," but the code's own naming didn't match). Renamed the file to `nominal_route_crossing.py` and the function to `nominal_route_constraint_crossing_time`, updated every reference/printed-output string in `exp3_interaction_force.py` and this README to match (`t_failure` → `t_crossing`, "Ground-truth failure time" → "Nominal-route constraint-crossing time (model-predicted)", etc.). Pure rename, no behavior change — re-verified `exp3_interaction_force.py` still parses correctly and all references are consistent (`grep` confirms no stray old-name references outside intentional "renamed from" explanations).
23. **Goal-execution fragility root-caused and partially mitigated** — see "Known environmental gaps" for the full writeup (two compounding causes: a whole-group aggregate progress-gate deadlock, and real torque saturation on the low-budget wrist joints from PID gains copied verbatim from the real robot's own config). New `b3.progress_stall_timeout_s` param (default 1.0s, a stall-timeout fallback) plus an empirically-tuned `FR3_LOCAL_VEL_SCALE=FR3_LOCAL_ACCEL_SCALE=0.3` took `"large"`'s own `final_pos_error_rad` from ~1.5-1.58 rad down to 0.494 rad (~3x improvement) — real and verified, but `task_success` still doesn't reach `True`. Also added: a new `B3_DEBUG_PROGRESS=1` debug log (same gating pattern as `B3_DEBUG_HORIZON`) tracing `current_duration_`'s own advancement decision directly, which is what made the root-cause diagnosis possible in the first place. No regression to any already-working goal (re-verified `small`'s `min_margin=9.6489` and Exp3's own flagship `t_detect=0.360` unchanged). **The stall-timeout fallback is unconditional (no opt-out), and Exp2 uses `"large"` for every cell — re-ran the full sweep to check.** Qualitative finding fully intact (B3 still triggers first at 6kg, B2 at 10kg, certificate lead still 4kg), but some mid-payload `min_margin` values shifted beyond ordinary run-to-run noise (5kg: 3.208→3.046, 6kg: 1.911→1.725 N·m; 0/3/7/8/10kg all within ~0.05 N·m of before) — B3 now progresses marginally further along the route before the stall-timeout intervenes, occasionally surfacing a slightly worse minimum that wasn't reached before. Disclosed honestly rather than left as stale numbers; the paper-ready conclusion (4kg certificate lead) is unaffected.
24. **A third, independently-verified external review found two more real issues, both fixed.** (a) `validate_prediction.py`'s alignment used `s[0] >= t_i` (the prediction cycle itself could be selected as its own "later observation") and a `dt*2` acceptance window (40ms — generous for a paper claiming prediction accuracy). Fixed to `> t_i` (strict) and `dt/2` (the natural bound given samples are spaced ~`dt` apart) — re-verified on the same force-disturbance bag: 2 predictions now validate (down from 3, since the previously-included one no longer meets the stricter window), RMSE/MAE both 0.1672 N·m. (b) Exp3's `t_detect`/`T_crossing` were both measured relative to goal ACCEPTANCE, not real execution start — each run has its OWN, slightly different planning latency, contaminating `T_warning`'s cross-run comparability. Fixed with `find_execution_start_offset()`, re-anchoring both to that run's own FIRST `/diagnostics` message's `header.stamp` (sim time, the same clock `force_t0` and `first_detection_time` already use) — purely in offline analysis, no change to the live force-injection trigger itself. **Caught and fixed a real bug of my own while implementing this**: the first attempt used `/global_trajectory`'s own bag-recording-time metadata as the execution-start proxy, which is a DIFFERENT, wall-clock-based reference incompatible with `force_t0`'s sim-time — mixing them gave a nonsense `T_crossing≈-1.8e9s` before being caught and corrected. After the fix: `execution_start_offset_s≈0.018s` (an 18ms correction, matching the small `planning_latency_s` values already established), and re-running twice gave `t_detect=0.342s` (exact match both times) and `T_warning=0.384-0.386s` — consistent with the previously-reported `0.383-0.391s` range, within ordinary noise.
25. **That same review's three "strongly recommended" items, all implemented.** (a) Re-verified `m_phys_observed`'s own C++ implementation line by line (the review couldn't check this from a Python-only zip) — confirmed correct: `observed_state` is a genuine copy of the real measured `current_state`, never `local_trajectory`'s own reference. Found one precision gap, not a bug: the force input is the SAME commanded/modeled force used for `m_phys`'s own prediction, not independently measured — updated both the C++ and `validate_prediction.py`'s own comments to state the precise claim ("prediction of future actuator margin under measured robot-state evolution," not "complete independent validation of the force model"), per the review's own suggested framing. (b) New `summarize_by_horizon()` in `validate_prediction.py`, grouping results by `horizon_s` and reporting RMSE/MAE/mean-error per bucket — live-verified on a real bag: 4 predictions, all sharing `binding_step=14` (one bucket, n=4, RMSE=0.1692). (c) `validate_predictions()` is no longer a separate, easy-to-forget manual step — `exp3_interaction_force.py`'s own `main()` now calls it on B3's own bag as part of the SAME primary report, immediately after the `T_warning` table. Live-verified the full combined pipeline end-to-end: `T_warning=0.396s` (B3), followed automatically by `4 predictions validated -- RMSE=0.1724 N·m, MAE=0.1630 N·m` and the per-horizon breakdown, all in one run.
26. **Second attempt at a COMPLETE goal-execution-fragility fix — found a third real root cause, substantial further progress, still not fully reliable.** See "Known environmental gaps" for the full writeup. Summary: `getTrajectoryProgress()` used to report "done" from reference progress alone, abandoning a genuinely-converging robot mid-correction (confirmed: a run at `0.107` rad error got abandoned and drifted to `0.701` with nothing left correcting it) — fixed to require real convergence too. Combined with retuning the low-torque wrist joints' own PID gains (`fr3_joint5/6/7`, cut ~5x from the real-robot-derived defaults that were saturating their `12` N·m budget on any real tracking error) and the already-known `FR3_LOCAL_VEL_SCALE=0.3`, `"large"`'s own `final_pos_error_rad` reached as low as `0.094` (just above the `0.06` success threshold) with genuine accelerating convergence, not a plateau. Real, substantial, verified progress — but NOT a reliable fix: a second otherwise-identical run landed at `0.182`, real run-to-run physics/timing variance that further tuning didn't eliminate (`open_loop_control: true` and reducing the base joints' own gains were both tried and made things worse). No regression: re-verified `small`, Exp3's flagship result, and a full Exp2 re-run all unchanged within ordinary noise.
27. **Third pass: root-caused the "large" goal's own oscillation (user-observed live, then quantified), fixed one real bug found along the way, found actuator-effort saturation as the true driver.** See "Known environmental gaps" for the full writeup, including the decisive `controller_state`-effort evidence. Two independent, real fixes came out of this pass and are both kept (neither resolves the oscillation on its own, but both are genuine correctness/tuning improvements): (a) `HorizonTrajectoryOperator::getLocalTrajectory()`'s horizon states were carrying a STALE velocity (frozen at the reference trajectory's own waypoint-0 value, ≈0) for every horizon step beyond literally the first sample of the whole route, because `RobotState::interpolate()` (called inside `getStateAtDurationFromStart`) only ever writes `position_`, confirmed directly against the vendored `robot_state.h` — fixed by linearly blending real velocities from the reference trajectory's own bracketing waypoints (which DO carry correct TOTG-computed velocities) via `findWayPointIndicesForDurationAfterStart`. This was silently corrupting `computeMPhysOverTrajectory`'s own certificate math too, for every horizon step past the first. (b) Base-joint PID gains (`fr3_joint1-4`) retuned `p: 600→300` (unchanged `d: 30`) — a genuine, if modest, verified improvement: matched 60s A/B/C trials at P:600/300/150 (same `FR3_LOCAL_VEL_SCALE=ACCEL_SCALE=0.3`, same 50Hz republish) showed P:300 reduces extrema/sec on the joints it touches (joint2: 57.1→45.2/sec, joint4: 37.6→30.8/sec) and slightly improves `final_pos_error_rad` (0.261→0.246), while pushing further to P:150 reverses the trend (extrema rate plateaus/rises again, `final_pos_error_rad` worsens to 0.277 — too weak to track, not just less oscillatory). **P:300 is now the repo's default** (`fr3_ros_controllers.yaml`), not reverted.

28. **Fourth pass: attempted torque-aware retiming and feedforward, both requested explicitly as the "real fix" — both confirmed, with real evidence, incapable of resolving this specific oscillation.** See "Known environmental gaps" for the full writeup and reasoning. Kept: a genuine new capability, `HorizonTrajectoryOperator`'s route-level pre-retiming pass (`b3.pretrack_relative_margin`, default `0.5`) targeting a RELATIVE (per-joint-budget-fraction) torque margin via new `computeMPhysOverTrajectory(..., min_relative_margin)` and `searchRetimeLambdaRelative()` — reuses the existing, already-tested `retimeTrajectory`/grid-search machinery, verified as a true no-op on `"small"` (`min_margin=9.64891` exact match, `task_success=True`, unchanged). Not kept (reverted, doesn't work): an `ff_velocity_scale` gain attempt on `fr3_ros_controllers.yaml` — real ROS2/`joint_trajectory_controller` parameter-loading issue found (both the nested `gains.<joint>.ff_velocity_scale` form, matching the exact upstream v2.53.1 `generate_parameter_library` schema, AND the deprecated flat `"ff_velocity_scale/<joint>"` form the controller's own warning suggests, failed to actually load — warning persisted either way, not root-caused further since the mechanism can't fully fix the problem regardless, see below).
29. **Fifth pass: built a real, working gravity+Coriolis feedforward controller — genuinely ELIMINATES the oscillation (a first, after every mechanism tried in passes 3-4 failed to), plus a partial convergence improvement, plus one clearly-diagnosed, separate, still-open issue.** New package `fr3_gravity_ff_controller` (`controller_interface::ControllerInterface` plugin, effort command interface, position+velocity state interfaces — a drop-in replacement for `fr3_arm_controller`, opt-in via `FR3_ARM_CONTROLLER_YAML=fr3_ros_controllers_gravity_ff.yaml`, default unset = stock `joint_trajectory_controller`, unchanged): reads the same `/fr3_arm_controller/joint_trajectory` stream B3 already publishes, computes `effort = bias(q,qdot) + Kp*pos_error + Kd*vel_error [+ Ki*integral_error]` each cycle, where `bias` (Coriolis+gravity) comes from a small self-contained KDL dynamics helper (`KdlChainDynamics`, NOT `fr3_dynamics`/`moveit_core` — see below for why). Three real, non-obvious build/runtime issues found and fixed along the way, all confirmed via direct evidence, not guessed:
    - `get_node()` on a `ControllerInterface` returns `rclcpp_lifecycle::LifecycleNode::SharedPtr`, not `rclcpp::Node::SharedPtr` — a real build failure, not assumed; `fr3_dynamics::FrankaChainDynamics::initialize()` needed a node-type-agnostic overload (URDF string directly) to be callable from a controller at all.
    - `fr3_dynamics` depending on `moveit_core` made the resulting `.dylib` unloadable specifically by `mujoco_ros2_control_node` (a `dlopen` "symbol not found in flat namespace '_PyExc_RuntimeError'" error) — B2/B3 load fine in `component_container_mt`, a different process that apparently already has Python loaded transitively; the lean `mujoco_ros2_control_node` does not. First suspected `moveit_core` specifically and removed it (wrote `KdlChainDynamics`, a small self-contained duplicate of `fr3_dynamics`'s own KDL setup, avoiding `moveit_core` entirely) — the SAME error persisted, proving that hypothesis wrong. Root cause via `otool -L`: `ament_target_dependencies()` links each ROS message package's FULL exported interface, which on this distro includes each package's own `*_rosidl_generator_py` library (real CPython C-API bindings needing `libpython3.12` at load time) — confirmed the officially pre-packaged `joint_trajectory_controller.dylib` does NOT link any of these at all, while this new package's build did. Fixed by linking `libpython3.12.dylib` directly (present in the conda env's own `lib/`, just not on the default link path) rather than fighting which targets get linked.
    - Same `-latomic` link issue as `mujoco_ros2_control_demos` (an unrelated, pre-existing, out-of-scope package under the vendored `mujoco_ros2_control` clone) — `rcutils`'s own exported `INTERFACE_LINK_LIBRARIES` bare-names `"atomic"` unconditionally, which macOS's linker can't resolve via the default search path even though `libatomic.dylib` exists in the conda env. Fixed the same way as the Python fix: point the linker at the env's own `lib/` directly (derived from where `rclcpp` was actually found, robust to `$CONDA_PREFIX` resolving to the wrong (base) env in this shell setup).

    **Live-verified results** (large goal, `FR3_LOCAL_VEL_SCALE=ACCEL_SCALE=0.3`, P:300/etc. gains unchanged from #27, same config throughout): PD+feedforward alone (no integral, `i:0` matching the stock controller) took extrema/sec from the established 22.5-57.1 baseline down to **0-0.45/sec** — the oscillation is genuinely gone, confirmed on both a 60s and a 200s run. But `final_pos_error_rad` plateaued at 0.83-0.89 (worse than #27's own 0.246-0.261, since progress now stops cleanly rather than the old chatter occasionally helping it along) — every joint's tracking error froze at a fixed nonzero value once the reference route completed (~75-100s in), never closing further even after 100+ more seconds. Root cause: **pure PD (`i:0`, both old and new controller) has no mechanism to eliminate steady-state error under any constant disturbance** (friction, or feedforward-model mismatch) — previously masked by the oscillation's own erratic, saturating swings, which apparently delivered enough intermittent forcing to occasionally make progress despite never truly converging. Confirmed directly: joint7 sat at a perfectly constant, sub-threshold 4.06 N·m effort (well under its 12 N·m budget) with velocity/position bit-for-bit frozen for 100+ seconds.

    Added a real integral term (anti-windup: clamps the term's own torque contribution, then re-derives the accumulated-error state from the clamped output, not just the output itself) — genuine, if partial, improvement: `final_pos_error_rad` 0.829→0.746, most joints (1,2,3,4,5,6) showing further real progress. Reintroduced mild oscillation on some joints (up to ~9.5/sec — still far below the original 22.5-57.1 baseline). **Two separate, now clearly-diagnosed reasons full convergence still isn't reached**, neither fixable by more integral gain: (a) base joints (1-4, `p:300`) are genuinely torque-SATURATED — confirmed live, effort pinned at ±85-87 N·m — driven by `p * residual_error` alone once error exceeds ~0.29 rad at this gain, independent of feedforward; more integral just competes for the same already-exhausted budget. (b) joint7 specifically stays almost completely frozen (0.400→0.396 rad error over 200s) even with effort raised to 7 N·m (still well under its 12 N·m budget, ruling out "insufficient torque") — self-collision was the leading hypothesis at the end of this pass; **ruled out and replaced by a confirmed real-torque-threshold explanation, see #30**.

    **No regression**: re-verified `"small"` with the final config (feedforward + integral) — `min_margin=9.648983` (exact match to the `9.64891` baseline), `task_success=True`, `final_pos_error_rad=0.0496` (matches baseline `~0.05`) — the new controller is a strict improvement over the stock PD controller on every metric checked on `"large"`, with zero cost on the already-working case. **`fr3_gravity_ff_controller` is the recommended controller going forward** but is opt-in (`FR3_ARM_CONTROLLER_YAML`), not yet the default, since `"large"`'s own `task_success` still isn't reached and joint7's stall (see #30) needs a fix beyond what any controller running within its real torque budget can provide.

30. **[Root cause superseded by #33 — see there before reading this] Root-caused joint7's stall directly in MuJoCo, independent of the ROS2 stack or any controller code — a real, reproducible torque threshold, not self-collision or friction.** (Self-collision and friction were, and remain, correctly ruled out below; what's superseded is the "real physics" attribution for the threshold itself — it was the undiagnosed `fr3.xml` position-actuator conflict, capped at this exact per-joint budget, per #33.) Loaded the exact recorded stall configuration (`/joint_states` at t=150s of the #29 integral-term run) directly into `mujoco` (Python bindings, `fr3_mujoco_bringup/mujoco_models/fr3.xml`) and tested hypotheses in order of likelihood, each checked against real simulation output, not assumed:
    - *Self-collision*: `mj_forward` at the exact stall pose gives `ncon=0` — zero contacts anywhere. Ruled out.
    - *Joint limit*: joint7 sits at `q≈0.002` against a `[-3.0159, 3.0159]` range — 3+ rad of margin either side. Ruled out.
    - *Coulomb friction* (`frictionloss`, `0.248` on joint7's own `<joint>`, `1.137` in the `class="fr3"` default other joints inherit): stripped ALL `frictionloss` from the model (verified via `grep -c` that zero instances remained) and re-ran the identical test — **identical result** (`delta=0.00146` either way, to 5 decimal places) — friction contributes essentially nothing here. Ruled out.
    - **Real mechanism, confirmed by direct simulation**: stepped the model forward with joints 1-6 held near their real stall position (PD hold matching the live controller's own gains, gravity-compensated every step — an earlier, flawed first attempt that let joints 1-6 free-fall under zero applied force was corrected before drawing this conclusion) while sweeping joint7's own applied torque in isolation. Result: **4-12 N·m (joint7's entire real torque budget) moves it by less than 0.007 rad before velocity hits exactly zero within ~1s and stays there** (tested out to several more seconds); **20 N·m (beyond the real budget, diagnostic only) moves it the full 3+ rad range cleanly**. A genuine, sharp, reproducible torque threshold between 12 and 20 N·m exists at this pose — confirmed independent of `fr3_gravity_ff_controller`, `mujoco_ros2_control`, or any ROS2 code at all, since this test bypassed the entire stack and drove `qfrc_applied` directly.
    - **Damping/inertial-coupling mechanism, isolated in a follow-up pass.** Ablated `damping="0.21"` (the remaining untested passive-force parameter, `class="fr3"` default, inherited by joint7) both on joint7 alone and globally — **zero effect either way**, identical to baseline at both 7 and 12 N·m: damping is definitively ruled out too, alongside friction. Ablating `armature="0.074"` (joint7's own inertia-shaping term) revealed the real structure instead: a **sharp torque threshold between 11 and 12 N·m, independent of armature**. Below it (7-11 N·m, with or without joint7's own armature), the response is identical every time — a perfectly linear creep (`Δq = 0.0005 rad × torque N·m`, settling within 0.6s) then a hard stop at exactly zero velocity. Above it, armature determines the outcome: with joint7's default armature, even 12 N·m barely moves it (+0.0066 rad, same small-plateau pattern); with joint7's armature zeroed, the identical 12 N·m breaks cleanly through — a 60s run shows genuine large-scale, oscillating/settling motion (+2.03 rad), not another plateau. **Conclusion**: not a single passive-force parameter — a real threshold effect in the coupled-system dynamics (most likely the mass-matrix interaction between joint7 and the rest of the arm, held at finite PD stiffness rather than perfectly rigid in this test, and in the real controller too), where armature governs how decisively the joint escapes once torque crosses the threshold, but doesn't itself explain where the threshold sits. The threshold's own root cause (checking mass-matrix coupling terms directly at this pose) was not traced further this session — flagged honestly as the boundary of this investigation, not blocking the conclusion below, which holds regardless: within the REAL 12 N·m budget and REAL (unablated) armature, joint7 cannot move through this configuration.
    - **Live-verified with the real closed-loop system, not just the isolated open-loop test — confirms and refines the finding above, doesn't unblock joint7.** Zeroed `damping`/`armature` globally in a new `fr3_no_damping_armature.xml` (opt-in via `FR3_MUJOCO_MODEL_FILENAME`, default unset = real unmodified model, a true no-op — added only to `fr3_b3_demo.launch.py`, the launch file actually used for this test) and re-ran the full "large" goal (gravity+Coriolis feedforward + integral controller, 200s). Joint7 stayed completely stuck — `0.400→0.397` rad error, effort still only `6.84` N·m at t=150s (essentially unchanged from the real model) — because the real closed-loop controller's own commanded torque for joint7 never approaches the `11-12` N·m threshold the isolated test found necessary for armature-removal to matter at all; the ablation genuinely doesn't help below that threshold, and the controller never gets there. Base joints (1-4) improved modestly (`final_pos_error_rad` `0.746→0.725`, ~3%) — plausibly reduced effective inertia letting the SAME already-saturated 87 N·m torque accelerate the arm slightly more efficiently, not escaping saturation itself (still pinned at exactly `87.000` N·m, unchanged). No oscillation regression — most joints' extrema/sec actually dropped further (joint7: `9.47→5.90`, joint3: `8.78→4.64`, joint5: `4.09→0.69`). **This is a non-physical diagnostic model change, not a candidate fix** — kept as a documented, reproducible ablation artifact, not wired in as a default anywhere.
    - **Follow-up: drove joint7's real commanded torque toward its true 12 N·m ceiling instead, with the REAL (unablated) model — this DOES cross the threshold, partially.** New opt-in config `fr3_ros_controllers_gravity_ff_j7boost.yaml` (`FR3_ARM_CONTROLLER_YAML`, identical to the working feedforward+integral config except joint7's own `p: 10→35`, `i_clamp: 3→5`, sized so `p * (its own ~0.4 rad steady-state error)` alone approaches/exceeds the real budget and gets pinned there by `mujoco_system.cpp`'s own real clamp). Live-verified (200s, "large" goal): **joint7 actually moved this time** — error dropped `0.400→0.238` by t=60s (effort `12.1-12.2` N·m, genuinely at the ceiling; velocity a real, nonzero `0.002-0.003` rad/s, not the numerical-zero seen in every prior test) — then re-settled at a NEW stuck point (`~0.212` rad error, effort back to `11.96` N·m, velocity back to zero) rather than reaching the target. `final_pos_error_rad` improved to `0.657` (vs. `0.746` baseline, `0.725` with the damping/armature ablation) — the best result of any configuration tried this session. No meaningful oscillation regression elsewhere; joint7 itself oscillated LESS (`9.47→1.67` extrema/sec).
    - **Refined conclusion**: sustained near-maximum real torque DOES cross the first threshold and produces genuine progress — this isn't a single, permanently un-crossable block, but more likely a landscape with multiple quasi-stable configurations along joint7's path (consistent with the isolated open-loop test's own long-run behavior, which also showed real motion through an arc with intermediate settling/reversal, not one clean jump straight to the target).
    - **Extended-time follow-up, decisive: more time does NOT help — this is a genuine, stable, permanent second equilibrium, not slow convergence.** Re-ran the SAME `j7boost` config with a 500s timeout (2.5x the earlier 200s run). Result: `final_pos_error_rad=0.666`, essentially identical to the 200s run's `0.657` (within noise) — `task_success` still `False`. Checked every joint's error at 12 checkpoints across the full 500s: **every joint reaches its own settled value by t=100-150s and then stays bit-for-bit identical (to 3 decimal places) for the remaining 350+ seconds** — joint7 specifically lands at `0.205` rad error (matching the 200s run's `0.212`) and never moves again, no drift, no oscillation, no further progress whatsoever. This rules out "just needs more time/patience" cleanly — closing the remaining gap needs an ACTIVE additional intervention (e.g. a still-higher sustained torque near/at the true ceiling for base joints too, a smarter time-varying gain schedule, or accepting `"large"` design should avoid this specific joint7 pose), not a longer timeout.
    - **Base joints (1, 4) checked with the same methodology — already at their real ceiling, and even that ceiling is marginal.** Confirmed live first: base-joint effort in the `j7boost` equilibrium is pinned at `87.000`/`-85.171`/`86.041`/`-85.094` N·m (joints 1-4) — already at, or within `1-2` N·m of, their real `87` N·m budget. Raising `p` further is a dead end: it cannot deliver more torque once the hardware clamp is already engaged. Ran the SAME isolated open-loop threshold test used for joint7 (held-arm, target joint swept in isolation) on joint1/joint4 instead: found an extremely narrow escape window — `85` N·m stays stuck (small creep, same pattern as joint7), but `87` N·m (their exact real ceiling) breaks cleanly through, swinging the joint all the way to near ITS OWN physical limit (`joint1: q→2.75` against a `2.7437` upper limit; `joint4: q→-2.98` to `-3.05` against a `-3.0421` lower limit) — far past the actual goal target (`0.6`/`-0.9` rad). **A real, unresolved discrepancy**: the isolated test says `87` N·m should just barely cross the threshold, yet the LIVE system — confirmed reaching that same `87.000` N·m — does not escape. Most likely explanation (not confirmed): real transient/timing dynamics the idealized static-hold isolated test doesn't capture (network/control-loop jitter, the real controller's own velocity feedback during approach, or joint7 and a base joint both sitting near their own thresholds simultaneously and interacting) — flagged honestly as open, not resolved. **Even if crossed, this would not be a useful outcome**: escaping swings the joint toward its own limit, not toward the actual goal — a genuine overshoot, not a fix.
    - **Practical conclusion**: this is real physics in the shipped MJCF model, not a bug in `fr3_gravity_ff_controller`, `mujoco_ros2_control`, or the goal-execution-fragility fixes from passes 1-5, and it now applies to BOTH joint7 and the base joints, not one quirky outlier. No controller — PD, the new feedforward+integral controller, or a hypothetical MPC (see the pHRI `impedance_mpc.py` assessment earlier this session, which also has no collision/contact model and would hit the identical wall) — can move these joints through this configuration within their real torque budgets, and even the (marginal, unconfirmed-live) theoretical escape for base joints would overshoot toward their own physical limits rather than reach the goal. `"large"` sits right at this platform's real physical/safety boundary — treat it as demonstrating that boundary, not as a controller-tuning problem to keep chasing. A genuine fix needs either the MJCF model's own damping/inertial parameters revisited, or (more practically) future goal design that stays within the range this platform reliably and safely handles.

31. **Second half of the #27 velocity-interpolation fix — acceleration had the SAME stale-value bug, missed at the time, caught in review.** `HorizonTrajectoryOperator::getLocalTrajectory()`'s per-cycle online horizon (`local_trajectory`) had its velocity fixed in #27 by blending real values from `reference_trajectory_`'s bracketing waypoints instead of trusting `RobotState::interpolate()` (which only ever writes `position_`) — but the exact same fix was never applied to `state_j`'s ACCELERATION, left stale at whatever waypoint 0's own (near-zero, routes start at rest) acceleration was, for every horizon step past the first. Consequential: `computeMPhysOverTrajectory` (`torque_margin_certificate.cpp`) reads `qddot` directly into `tau = mass*qddot + bias` — the dominant, unscaled term for any fast-accelerating motion, more consequential than the Coriolis (`qdot`) contribution the #27 fix addressed. Fixed identically: blend real acceleration between the reference trajectory's bracketing waypoints via the same `findWayPointIndicesForDurationAfterStart` call already used for velocity.
    - **Scope of what this actually affects, checked directly rather than assumed**: `reference_trajectory_` itself (populated by `TimeOptimalTrajectoryGeneration::computeTimeStamps()` in `addTrajectorySegment()`) always had correct accelerations — never went through the buggy code path. This means the fourth pass's own `searchRetimeLambdaRelative`/`computeMPhysOverTrajectory` calls (which operate on `reference_trajectory_` and its `retimeTrajectory()`-derived candidates directly, bypassing `getLocalTrajectory()` entirely) were NEVER affected by this bug — the "torque-margin retiming is structurally incapable, capped by gravity" conclusion stands unchanged, re-confirmed live (`"large"` goal, `B3_DEBUG_HORIZON=1`): `[pretrack]`/`[whole-route]` step margins are unchanged to within 0.001-0.002 N·m of the pre-fix values. What DID change: the ONLINE per-cycle certificate (`B3ConstraintSolver`'s own `"horizon"`-tagged `computeMPhysOverTrajectory` call on `local_trajectory`, published as `m_phys` to `/diagnostics`, driving the real-time Level 0/2/4 decision) — confirmed via a live re-run, `m_phys` now ranges `9.615-9.686` vs. the earlier stale-acceleration run's `9.649-10.00` (tighter, generally lower, consistent with acceleration genuinely adding to required torque mid-trajectory rather than being understated as ~0). Still far above `m_safe=2.0`, so the online Level 0/2/4 decision itself is unaffected (still always Level 0) — a real correctness improvement to the certificate's own accuracy, not a behavior change.
    - **No regression on `"small"`, but the certificate's own reported number did shift** — worth flagging explicitly since `min_margin=9.64891` had been used all session as the standard "verified true no-op" checkpoint: re-verified `task_success=True`, `final_pos_error_rad≈0.05` (both unchanged), but `min_margin=9.61958` now, not `9.64891` — a real, if small (~0.03 N·m), change from the fix, not noise. **`9.61958` is the new correct baseline for `"small"` going forward**; `9.64891` was itself always a product of the (now-fixed) stale-acceleration bug and should be treated as superseded, not as a target to reproduce.

32. **A fourth, independently-verified external review of `scripts/` — one real timestamp bug fixed (with a controlled before/after comparison), one real labeling error corrected, one genuine experimental-design confound identified and disclosed.** Every claim checked directly against the code before acting on it, not assumed correct because it sounded plausible — one of the six points raised turned out not to need a code change (Exp3's `T_warning`, already correctly scoped; see below).
    - **(a) `validate_prediction.py` was mixing two different clocks — real, fixed, verified with a controlled A/B test.** `t` in `for t, msg in diag_msgs` comes from `rosbag2_py.SequentialReader.read_next()` — rosbag2's own message-receipt timestamp. Confirmed directly: `run_experiment.py` invokes `ros2 bag record` with no `--use-sim-time` flag, so this is genuine wall-clock time, while `b3_constraint_solver.cpp` sets `diag_msg.header.stamp = node_->now()` on a node with `use_sim_time: true` (every launch file), making `header.stamp` genuine sim time — the SAME clock `binding_step*dt` (a sim-time-domain horizon) is already in. This is the exact bug class `exp3_interaction_force.py`'s own `find_execution_start_offset`/`first_detection_time` already guard against (and already correctly use `header.stamp` for) — `validate_prediction.py` was simply the one script not yet updated to match. Fixed by switching to `msg.header.stamp.sec * 1_000_000_000 + msg.header.stamp.nanosec`. **Verified with a controlled comparison, not just "re-ran and got a different number"**: ran BOTH the old (bag wall-clock) and new (sim-time `header.stamp`) alignment logic against the exact SAME fresh B3 bag. Old: `n=3` predictions validated, `RMSE=0.1696`, `MAE=0.1614` N·m. New: `n=4` validated (the fix correctly includes one more prediction the old wall-clock alignment excluded), `RMSE=0.1895`, `MAE=0.1809` N·m. A real, non-trivial (~12%) shift — NOT "nearly identical," confirming this needed fixing rather than being safe to assume away. **`RMSE≈0.19` N·m (not the earlier `0.1672`) is the number to use if/when this validation makes it into the paper.**
    - **(b) `t_detect` was mislabeled "after force onset" throughout the README — it's actually time since local execution begins.** Direct algebra from `exp3_interaction_force.py::run_cell`: `t_detect = t_detect_raw - exec_offset = (stamp - force_t0) - (t_exec_start - force_t0) = stamp - t_exec_start` — time since execution start, not since the force disturbance itself begins (which starts at `force_t0 + FR3_FORCE_T_ONSET`, a DIFFERENT reference point). Confirmed on two independent runs that the actual gap from real force onset to detection is much smaller than the `t_detect` figure being quoted: the previously-documented run (`t_detect=0.342s`, `execution_start_offset_s=0.018s`) implies `0.342 - (0.3 - 0.018) = 0.060s` after actual onset; a fresh re-run this session (`t_detect=0.324s`, `exec_offset=0.034s`) gives `0.324 - (0.3 - 0.034) = 0.058s` — consistent across two independent runs, not a fluke. **`T_warning` itself is unaffected** (both `T_crossing` and `t_detect` share the same execution-start reference frame, so their difference is invariant to which point is "zero") — only the standalone `t_detect` figure's own description needs correcting, everywhere it's currently phrased as "after force onset." Fixed in the summary line above; the remaining older occurrences (`scripts/exp3_interaction_force.py`'s own description, the Phase 5 next-step entry, and the Phase 4c-fix/`#12`/`#24` historical entries using the superseded `0.360s`/`0.342s` figures) are left as historical record with this entry as the correction pointer, per the "don't rewrite retracted numbers, flag the correction" principle — future runs quoting `t_detect` should describe it as "time since local execution begins," and separately compute the true offset from real force onset if that specific number is needed.
    - **(c) Exp2's "4kg lead" is a real, reproduced finding, but "prediction gives a 4kg lead" overclaims it — confirmed via direct code comparison, not just a wording preference.** Read `b2_constraint_solver.cpp`'s own trigger condition directly: `exceeded = std::abs(tau_nominal(i)) > tau_max_(i)` — B2 intervenes only once torque LITERALLY exceeds the hard limit, with **zero safety margin**. B3's trigger (`torque_margin_certificate.cpp`) is `m_phys < m_safe_` where `m_phys = tau_max - |tau| - delta_tau` — B3 requires clearing `m_safe(2.0) + delta_tau(5% of tau_max)` of buffer before intervening, roughly `6.35` N·m of headroom for base joints (`87` N·m budget) that B2 doesn't require at all. **This means the 4kg payload gap conflates two genuinely separate effects**: B3's predictive horizon (the paper's actual claim) AND B3's much more conservative trigger threshold (a purely reactive/non-predictive difference — B3 would trigger earlier than B2 even evaluated at a single current instant with zero lookahead, simply because it demands more margin). The paper's own "B2-vs-B3 fairness principle" (`Sec. VIII-A`: "both share the same controller, differing only in reactive-vs-predictive") is not fully upheld by the current implementation — the two baselines share the same DYNAMICS MODEL but not the same SAFETY THRESHOLD. **Recommended framing for the paper** (matches the external review's own suggestion, now with the mechanism confirmed): describe this as "B3's predictive safety certificate activates 4kg earlier than B2's reactive torque-limit handler," not "prediction provides a 4kg lead" — the honest claim is activation-threshold separation, not an isolated measurement of prediction's own contribution. Isolating the TRUE predictive-only contribution would need a follow-up experiment giving B2 the same `m_safe`/`delta_tau` margin B3 uses, evaluated at the current instant only — not attempted this session (scope creep on an already-large session; flagged as a real open item instead).
    - **(d) `T_warning` as a metric, and `validate_prediction.py` as a metric, measure genuinely different things — both are legitimate, described precisely as what they measure, not what they don't.** `T_warning = T_crossing - T_detection` mixes the certificate's OWN future-margin prediction with its OWN safety-buffer anticipation (since `T_crossing` uses `m < 0` while B3's own intervention condition uses `m_phys < m_safe`) — a valid, real "how much warning time did the whole system provide" metric, but not an isolated measurement of raw predictor accuracy. `validate_prediction.py` (predicted `m_phys` vs. later-observed `m_phys_observed` at the same absolute time) IS the more direct predictor-accuracy metric — which is exactly why fixing its timestamp source (item a) mattered more than any other single change in this review. No code change needed here — the distinction just needs to be respected in how each number gets described in the paper (already correctly scoped for `m_phys_observed` per the README's own existing "Real bugs found and fixed" #25a language, re-confirmed accurate on review).
    - **(e) Exp4: agreed, no action taken.** The review confirms the README's own existing treatment (trajectory determinism fixed, `"medium_slow"` itself still doesn't execute reliably, so Exp4 can't yet establish that contact transition specifically caused any B1/B2 failure) and explicitly recommends NOT spending further time polishing `exp4_contact_transition.py` itself — matches this session's own conclusion in the goal-execution-fragility writeup (design future goals to stay within the platform's demonstrated range, e.g. via `fr3_gravity_ff_controller`, rather than continuing to chase a specific fragile goal).
    - **(f) README organization (split into an evidence-record `README.md` + a `DEBUG_HISTORY.md` diary)**: a reasonable suggestion for future cleanup, NOT acted on this session — a large, purely-organizational restructuring of ~30+ numbered entries and a "Known environmental gaps" section, orthogonal to the technical fixes above, better done as its own deliberate pass (ideally checked with the user first, since it changes how every future entry gets filed) rather than folded into a bug-fixing session.

33. **A fifth external review found the actual root cause of the joint7/base-joint "stall" — retracting the "real physical/safety boundary" conclusion of #23/#26-#31 and the Sixth pass's own torque-threshold explanation.** `fr3.xml` shipped an `<actuator>` block of `<position>` servos (`kp=4500/3500/2000` for base/wrist joints) left over from what was evidently an earlier, pre-effort-control development phase — confirmed directly: `mujoco_system.cpp::write()` applies every commanded effort via `mj_data_->qfrc_applied[...]` and never once writes `d.ctrl[...]`, so those position actuators' targets stayed at MuJoCo's zero-initialized default the entire time, meaning every joint was fighting an invisible, un-modeled `kp*(0 - q)` restoring force on top of whatever the real effort controller (and, in every Phase-A "isolated" MuJoCo test this session, `qfrc_applied` set directly in Python) commanded. This is exactly what the earlier "sharp torque threshold" findings were actually measuring, not real rigid-body physics: MuJoCo clamps an actuator's realized force to the joint's own `actuatorfrcrange` (`±12` N·m for joints 5-7, `±87` for the base joints, confirmed via `model.jnt_actfrcrange`) — verified directly in a standalone script (`qpos[joint7]=0.388` → `qfrc_actuator=-12.0` exactly, not the raw `kp*error=776` N·m the uncapped servo wanted), so the hidden position-actuator's own opposing force saturates at PRECISELY the same "budget" the isolated tests kept finding, for the same structural reason in both cases, not by physical coincidence. Nothing in the codebase looks up actuators by name/index (`grep` for `mjOBJ_ACTUATOR` across the whole tree: zero hits) — confirmed safe to remove.
    - **Fix applied and independently re-verified live, not taken on the review's word alone**: removed the `<actuator>` block (and the now-orphaned `ctrl=` attribute on the `home` keyframe, which sized itself to the old actuator count) from `fr3.xml`. Model reloads cleanly (`m.nu == 0`). A fresh `"large"`-goal B3 run on the stock (non-feedforward) controller — the config every prior pass in this saga used — now shows **clean, monotonic, non-oscillating convergence** for the first time in this entire investigation: `final_pos_error_rad` 0.921 (30s) → 0.123 (120s) → 0.141 (200s), `min_margin` a healthy 9.61-9.61 throughout (never below 4), zero online interventions. This is categorically different behavior from every earlier pass (limit-cycle chatter, multiple stuck equilibria, 5-6 passes of increasingly complex mitigation) — the position-actuator conflict was clearly the dominant driver of the platform's own reported "physical boundary," not a real one.
    - **Does NOT fully reproduce, and is reported honestly rather than smoothed over**: the review's own pasted verification claimed `task_success=True`, `final_pos_error_rad=0.02630`, joint7 error `0.00198` on the SAME fix. Re-running this session's own methodology at 30s, 120s, and 200s goal-timeouts (`run_experiment.py --goal large --goal-timeout {30,120,200}`) instead shows the error PLATEAUING around 0.12-0.16 rad (120s: 0.1234, 200s: 0.1407 — slightly WORSE at 200s than 120s, not monotonically approaching zero) — `task_success` (threshold `0.06` rad) was not achieved in any of these three runs. The fix itself is real, decisively confirmed both analytically (direct MuJoCo introspection, above) and behaviorally (the qualitative change from oscillating/stuck to smoothly converging), but the review's own specific post-fix numbers were not reproduced with this session's own test harness and default controller config. Not yet root-caused: whether the remaining 0.12-0.16 rad plateau is (a) genuine run-to-run variance (this exact scenario had "real, substantial" documented variance even before this fix, see #29's fourth-pass writeup), (b) a second, smaller, still-unidentified factor, or (c) a difference in the reviewer's own test methodology (goal-timeout, controller config, or something else not stated in the pasted excerpt) — flagged as open, not guessed at.
    - **Four smaller, independently-confirmed defects from the same review, fixed and rebuilt (`colcon build --packages-select mujoco_ros2_control fr3_gravity_ff_controller`, clean)**: (a) `run_experiment.py`'s `run_one()` used to return normally even when the Hybrid Planner action was rejected, timed out, or completed with a non-`SUCCESS` MoveIt error code — confirmed every sweep script (`exp2/3/4_*.py`, `goal_fragility_variance.py`) already wraps `run_one()` in `try/except RuntimeError`, so raising here (now: `RuntimeError` on `not accepted` or `error_code != 1`, bag already written first) makes that existing handling path meaningful rather than dead code, without touching the separately-computed, bag-derived `task_success` metric. (b) `MujocoRos2Control::init()` had four internal early-return failure paths (URDF parse error, pluginlib load failure, hardware-interface init failure, missing `update_rate` param) but returned `void`; its one caller (`mujoco_ros2_control_node.cpp`) proceeded straight into rendering setup and the main loop regardless, and the destructor unconditionally dereferenced `cm_executor_`/`controller_manager_`, which would be null on a failed init. Changed to `bool`, added an `initialized_` flag set only past the real final success path, guarded the destructor on it, and made the caller check the return value and exit(1) with cleanup on failure. (c) `fr3_gravity_ff_controller/CMakeLists.txt` linked `python3.12` by literal name (needed at dlopen time by the `*_rosidl_generator_py` libs this package transitively pulls in — see this package's own existing comment for why `mujoco_ros2_control_node` specifically needs it explicit); replaced with `find_package(Python3 COMPONENTS Development REQUIRED)` + `Python3::Python`, robust to a future RoboStack Python version bump. Re-verified live after a clean rebuild that this doesn't silently regress the exact dlopen crash it was originally added to fix: launched with `FR3_ARM_CONTROLLER_YAML=fr3_ros_controllers_gravity_ff.yaml`, confirmed `"GravityFeedforwardController: on_init starting (plugin loaded and instantiated OK)"` in the log — no `_PyExc_RuntimeError`-style symbol failure, even though `otool -L` on the rebuilt `.dylib` no longer lists `libpython` as a direct dependency the way the old hardcoded link did (empirically fine on this setup, not re-derived from first principles why). (d) `mujoco_ros2_control_node.cpp`'s `main()` returned `1` after its own orderly post-loop cleanup (viewer closed, model/data freed) — a clean shutdown reported to `ros2 launch` as a failed process; changed to `return 0`.
    - **Not yet investigated**: the review's fifth defect claim (the standalone Hybrid Planning manager's fixed ~7s startup timer racing a 2s action-server wait on slow/cold startup) — grepped `fr3_mujoco_bringup`'s own launch files for the described timer and found no match; likely lives in the third-party `moveit_hybrid_planning` package (gitignored here, own clone) rather than this repo's own code. Not investigated further this pass.
    - **Consequence for every number in this README computed before this fix**: entry #30's own "joint7 has a real torque threshold ~11-12 N·m ... likely mechanism: damping + inertial coupling" explanation is superseded by this entry, not merely refined — the threshold was real and reproducible, but its cause was misdiagnosed. Numbers from Exp1/2/3/4 that were NOT sensitive to this bug (i.e., didn't depend on the arm reaching a large joint-space displacement — `"small"`/`"small_slow"`-goal results, in particular Exp3's detection-lead numbers) are not expected to change, since those goals never accumulated enough position error for the hidden servo's opposing force to matter; this has not yet been re-verified by rerunning them post-fix, and should be before treating this fix as fully folded into every reported number in this file.

## Known environmental gaps (disclosed, not hidden)

- **`mujoco_ros2_control` has a spontaneous crash window**, roughly 5–25s after launch, cause unknown (suspected GLFW/macOS window-server instability from repeated relaunches in one session — not caused by any patch here, confirmed via isolation testing). Mitigation: always poll for both the plugin-initialized log line AND the process being alive before sending a goal; don't idle between launch and verification.
- **No SCS solver in this environment** (checked directly — only `osqp`/`OsqpEigen` are present). The Python reference falls back from OSQP to SCS for the larger whole-route reshape QP; this port is OSQP-only. When OSQP can't converge on a big problem, the code reports it and falls through cleanly (matches the "retime exhausted" pattern) — it does not silently produce a wrong answer.
- **`m_safe = 2.0` N·m** (paper's own default, calibrated on a reduced-order 3-DOF arm) has never been validated as well- or poorly-calibrated at real FR3 scale. Not blocking, but flagged since Phase 3a.
- **B1 (stock ForwardTrajectory/SimpleSampler) cannot complete the "large" goal at all**, independent of payload (Phase 4b). Confirmed directly: at payload=0/3/5/7kg its joint_states show ~0.007 rad drift (sensor noise) for the *entire* 45s window — it never starts moving; at 10kg it moves substantially (1.78 rad) but still never reaches the target. ~~B2 and B3 both execute this exact goal fine at payload=0~~ — **superseded, see "Real bugs found and fixed" #16**: B3 shows the SAME "stuck, not just slow" signature at 0kg in this session's own latest Exp2 rerun (final error ~1.51 rad vs. an initial ~1.58 rad, effectively unmoved over 46s) — not yet re-checked for B2, and not yet root-caused for either. This was NOT caught by any prior phase's own verification (which only ever checked `min_margin`/intervention booleans for this goal, never `task_success`/`final_pos_error_rad` closely) — this is specific to B1's own stock local-planner pipeline on this particular goal, first surfaced in Phase 4b because it's the first time B1 was ever pointed at the large goal (Phase 4a's B1 check used the small goal). Root cause not investigated (out of scope — B1 was only needed here as a payload-sweep comparison point, not as something to fix); `exp2_payload_sweep.py` detects this case (B1 failing at the lowest payload swept) and prints a caveat instead of a misleading "certificate lead over B1" number. **Re-tested post-#33's actuator-conflict fix (0kg payload, 60s timeout): still broken, essentially unchanged** (`final_pos_error_rad=1.574`, action itself never even completed within 60s — `run_one()`'s new #33-fixed error handling correctly raised rather than silently returning). Since B3 on the identical model now converges cleanly post-fix while B1 still shows zero real movement, this confirms B1's own failure is a genuinely separate issue from the actuator conflict, not merely a consequence of it — still open, still not investigated.
- **`mujoco_ros2_control`'s macOS build needs `MUJOCO_DIR` set explicitly and `-DCMAKE_FIND_FRAMEWORK=LAST`** after any `--cmake-clean-cache` (Phase 4b, self-inflicted while adding the payload patch). Without the first, CMake can't find `find_package(mujoco)` at all; without the second, it resolves to the `mujoco.framework` bundle in the same `lib/` dir instead of the plain `.dylib` and fails to link (`ld: framework not found mujoco`). Only matters after a clean-cache rebuild of this one package — normal incremental builds are unaffected.
- **Goal-execution fragility for any single joint moved beyond ~0.13-0.15 rad from `"small"`'s own specific, previously-validated combination** (Phase 4c). **Root cause: an unused `<position>` actuator block in `fr3.xml` fighting the real effort controller — see "Real bugs found and fixed" #33.** Six passes were spent on this before that root cause was found (full detail in "Real bugs found and fixed" #23, #26-#31); most of that detail is now historical, but three real, independent fixes from that investigation remain valid and are NOT superseded by #33: a per-joint (not aggregate) progress-advancement fallback (`b3.progress_stall_timeout_s`), a fix making `getTrajectoryProgress()` require real convergence (not just reference-duration elapsed) before reporting done, and `fr3_gravity_ff_controller` (gravity+Coriolis feedforward, genuinely eliminates a separate oscillation problem, opt-in via `FR3_ARM_CONTROLLER_YAML`). What is superseded: the specific torque-threshold numbers (joint7 "11-12 N·m", base joints "at their 87 N·m ceiling") and the final "real physical/safety boundary, not a controller-tuning gap" conclusion — both were measuring the undiagnosed actuator conflict, not real rigid-body physics. **Current status, post-#33 fix**: clean, non-oscillating convergence, but `final_pos_error_rad` plateaus around 0.12-0.16 rad rather than reaching `task_success`'s `0.06` rad threshold — open, see #33's own three hypotheses for the plateau.
- **`TimeOptimalTrajectoryGeneration::computeTimeStamps()`'s response to `max_velocity_scaling_factor`/`max_acceleration_scaling_factor` is non-monotonic on `"small_slow"`'s own 2-waypoint path** (found while calibrating fix #12 above). An empirical sweep (`0.02` step size details in `exp3_interaction_force.py`'s own `FORCE_ENV` comment) found real route duration stays essentially flat (~0.32-0.34s) from scale 1.0 down to 0.05, then jumps abruptly to 0.61s at scale 0.02 — not the smooth, roughly-`1/scale`-ish physical response a velocity/acceleration-limited time-optimal parametrization should give. Suspected cause: the raw OMPL response for this goal is always exactly 2 waypoints (start+goal, `time_from_start=0` for both — confirmed by inspecting a captured `/global_trajectory` directly), a near-degenerate input for TOTG's own path-following algorithm; never investigated further (would need reading `time_optimal_trajectory_generation.cpp`'s own internals, a MoveIt-core third-party algorithm, not something this platform's own code controls). `exp3_interaction_force.py` uses `0.02` (the smallest value tried in that neutral, detection-outcome-blind sweep), not a value tuned against Exp3's own result. **Any future use of `FR3_LOCAL_VEL_SCALE`/`FR3_LOCAL_ACCEL_SCALE` on a different goal should re-sweep rather than assume a linear response.**

## How to build and run

```bash
# Activate environment (RoboStack ROS 2 Humble in a conda env)
source /opt/homebrew/Caskroom/miniconda/base/etc/profile.d/conda.sh
conda activate ros_env
cd /Users/yycao/ai_learn/replan/ros2_ws
source install/setup.zsh

# Build everything touched by this platform
colcon build --packages-select fr3_dynamics fr3_b2_local_planner \
  fr3_b3_local_planner fr3_replay_global_planner fr3_mujoco_bringup \
  moveit_hybrid_planning

# ALWAYS use a fresh ROS_DOMAIN_ID and kill stragglers before relaunching —
# DDS transient-local messages leak across launches otherwise.
pkill -9 -f "hybrid_planning|component_container|mujoco_ros2_control|robot_state_publisher|ros2_control_node|ros2 launch"
export ROS_DOMAIN_ID=<unused number>
ros2 launch fr3_mujoco_bringup fr3_b3_demo.launch.py   # or fr3_b2_demo.launch.py

# Poll for readiness before sending a goal (see "known gaps" above):
#   grep -q "B3ConstraintSolver initialized" <launch log> && pgrep -f mujoco_ros2_control
```

Test-config env vars (both launch files support these, default to the real values):
- `FR3_B2_TORQUE_LIMITS_YAML` — e.g. `config/b2_torque_limits_test_low.yaml`
- `FR3_B3_PARAMS_YAML` — e.g. `config/b3_params_test_low.yaml`, `config/b3_params_retime_test.yaml`, `config/b3_params_reshape_test.yaml`, `config/b3_params_reroute_test.yaml` (see `fr3_b3_local_planner/config/` for what each exercises)
- `FR3_PAYLOAD_MASS_KG` — end-effector payload in kg (Phase 4b), all three launch files support it, default `"0.0"`. Sets both the URDF and the MJCF's `"payload"` body/link so B2/B3's certificate and MuJoCo's simulated physics agree.
- `FR3_FORCE_MODE` (`""`/`"ramp"`/`"spring"`), `FR3_FORCE_T_ONSET`, `FR3_FORCE_RAMP_DURATION`, `FR3_FORCE_FX/FY/FZ`, `FR3_FORCE_CONTACT_Z`, `FR3_FORCE_K_CONTACT` (Phase 4c) — external end-effector force schedule, all three launch files support it, default disabled (`""`). Sets both the MuJoCo injection node's params and B2/B3's own `b2.force_*`/`b3.force_*` params from the same values. `FR3_FORCE_KNOWN_AT_PLAN_TIME` (B3's `fr3_b3_demo.launch.py` only) gates whether B3's route-level Level 1/2/3 search sees the schedule too (Exp4) or only the online path does (Exp3, the default).
- `FR3_GLOBAL_PLANNER_YAML`/`FR3_REPLAY_TRAJECTORY_PATH` (Phase 4c-fix) — all three launch files support it, default real unmodified OMPL planning (`config/hybrid_planning/global_planner.yaml`, `FR3_REPLAY_TRAJECTORY_PATH` unset). Set `FR3_GLOBAL_PLANNER_YAML=config/hybrid_planning/global_planner_replay.yaml` plus `FR3_REPLAY_TRAJECTORY_PATH=<path from scripts/capture_trajectory.py>` to replay one captured real trajectory instead of re-planning — used by `exp2_payload_sweep.py`/`exp3_interaction_force.py` so every cell/baseline in a sweep shares an identical geometric path + time law.
- `FR3_LOCAL_VEL_SCALE`/`FR3_LOCAL_ACCEL_SCALE` (Phase 4c-fix) — all three launch files support it, default `"1.0"` (a true no-op, matching every pre-existing baseline). Sets `local_velocity_scaling`/`local_acceleration_scaling` on the local planner node, actually threaded into `computeTimeStamps()` by both `SimpleSampler` (B1/B2) and `HorizonTrajectoryOperator` (B3) — see "Real bugs found and fixed" #12 for why this didn't exist before, and "Known environmental gaps" for why the response to it is non-monotonic (re-sweep before picking a value for a new goal, don't assume `1/scale`).
- `FR3_ARM_CONTROLLER_YAML` (fifth pass, "Real bugs found and fixed" #29) — all four `fr3_mujoco_bringup` launch files support it, default `fr3_ros_controllers.yaml` (the stock `joint_trajectory_controller`, a true no-op — unchanged for every existing result). Set to `fr3_ros_controllers_gravity_ff.yaml` to select `fr3_gravity_ff_controller/GravityFeedforwardController` instead — gravity+Coriolis feedforward on top of the same P/D(+I) gains, eliminates the goal-execution-fragility oscillation (not yet the default: `"large"`'s own `task_success` still isn't reached, see #29).

Debug instrumentation: set `B3_DEBUG_HORIZON=1` to get a per-waypoint margin dump (`RCLCPP_INFO`, tagged `[horizon]`/`[whole-route]`/`[online-reshape]`/`[whole-route-extended]`, each line also carries the waypoint's own absolute schedule time `t=` and FK `ee_z=`) — this is how every Phase 3b/3c/3d/4c test scenario was actually tuned against real dynamics numbers instead of guessed. Set `B3_DEBUG_PROGRESS=1` to trace `HorizonTrajectoryOperator`'s own `current_duration_` advancement decision each cycle (`current_duration=.../... dist=... tol=... within_tolerance=... stall_timeout_expired=...`) — this is what made the goal-execution-fragility root cause diagnosable (see "Real bugs found and fixed" #23). `run_experiment.py`'s `run_one()` now also always records `/fr3_arm_controller/joint_trajectory` (the real per-cycle commanded stream to the low-level controller) and `/fr3_arm_controller/controller_state` (`joint_trajectory_controller`'s own desired/actual/error/effort feedback) — previously unrecorded; adding these is what made the goal-execution-fragility oscillation's actual root cause (actuator-effort saturation, see "Known environmental gaps") diagnosable rather than guessed at.

Test scripts live in `scripts/`:
- `scripts/test_fr3_hybrid_planning.py` — small within-limits goal, the standard "zero interventions" regression check.
- `scripts/test_fr3_large_move.py` — large/fast goal, used to tune every Phase 3b/3c/3d verification scenario against real measured dynamics.
- `scripts/sample_progress.py` — sends the large move and samples `/joint_states` at a fixed elapsed wall-clock time (bounded window, so it doesn't need to wait for full completion or risk the crash window below); used to compare execution progress between runs (e.g. nominal vs. Level-1-retimed).
- `scripts/run_experiment.py` — Phase 4a's harness: launch + poll-ready + `ros2 bag record` (`/joint_states`, `/diagnostics`, `/rosout`, `/global_trajectory`) + send the standard goal + teardown, in one command. `python3 run_experiment.py --launch-file fr3_b3_demo.launch.py --bag-dir /tmp/some_run`. Add `--goal large` and/or `--payload-mass-kg 5.0` for Phase 4b-style runs. `run_one(...)` is the reusable entry point other scripts import (raises `RuntimeError` rather than exiting).
- `scripts/compute_metrics.py` — reads a bag `run_experiment.py` produced and prints `final_pos_error_rad`/`task_success`/`min_margin`/`online_intervention_count`/route-level event counts. `python3 compute_metrics.py /tmp/some_run [--goal large]`. `compute(...)` is the reusable entry point (returns a result dict); `read_bag(...)` is the lower-level all-topics reader other scripts reuse (e.g. for `/global_trajectory`).
- `scripts/capture_trajectory.py` — Phase 4c-fix: plans once via B1's own stock launch file (real, unmodified OMPL) and writes the resulting `/global_trajectory` to disk as raw serialized bytes, for `fr3_replay_global_planner/ReplayGlobalPlanner` to replay later. `python3 capture_trajectory.py small_slow /tmp/nominal.bin`. `capture_nominal_trajectory(goal, output_path, extra_env=None)` is the reusable entry point.
- `scripts/exp2_payload_sweep.py` — Phase 4b's payload sweep: runs B1/B2/B3 across a swept `FR3_PAYLOAD_MASS_KG` on the large goal (now via one captured+replayed trajectory, Phase 4c-fix), reports the payload at which each baseline first shows a violation vs. where B3's certificate first triggers ("certificate lead in kg"). `python3 exp2_payload_sweep.py [payload_kg ...]` (defaults to a coarse `[0, 3, 5, 6, 7, 8, 10]` sweep). Real result: certificate lead over B2 = 4kg (B3 triggers at 6kg, B2 at 10kg).
- `scripts/nominal_route_crossing.py` — Phase 4c (renamed from `ground_truth.py`, external review finding — see "Real bugs found and fixed" #22): reads a `B3_DEBUG_HORIZON=1` launch log's `"[whole-route-extended]"` lines (a debug-only certificate probe, see `horizon_trajectory_operator.cpp`) to compute the nominal-route constraint-crossing time (B3's OWN certificate model, evaluated offline, NOT independent ground truth) for the UNMODIFIED nominal route, matching `code/experiments/exp3_interaction_force.py::ground_truth_failure_time`'s own role in the Python reference. `nominal_route_constraint_crossing_time(launch_log_path)`.
- `scripts/exp3_interaction_force.py` — Phase 4c Exp3: unanticipated ramp disturbance (`force_known_at_plan_time=false`), reports `T_warning = T_crossing - T_detection` for B2/B3, now via one captured+replayed trajectory shared by nominal-route-crossing/B1/B2/B3, a genuinely-honored local re-time scale (Phase 4c-fix, both #11 and #12), both times re-anchored to real execution start rather than goal acceptance (#24), AND B3's own predicted-vs-observed margin validation folded into the same primary report (#25c). `python3 exp3_interaction_force.py`. Real, repeatedly-reproduced result: B3 detects `~0.32-0.34s` after LOCAL EXECUTION BEGINS (`t_detect` — NOT "after force onset," a labeling error corrected in #32b; actual force onset happens partway into that window, so detection is really `~0.06s` after the disturbance itself starts), `T_warning≈0.40-0.41s`; B2 never detects.
- `scripts/exp4_contact_transition.py` — Phase 4c Exp4: known-in-advance position-triggered contact (`force_known_at_plan_time=true`), reports task success/route-level events per baseline, now via one captured+replayed trajectory shared by B1/B2/B3 (see "Real bugs found and fixed" #19). Written but **not currently usable** — blocked on the goal-execution-fragility issue in "Known environmental gaps" (no goal with meaningful end-effector Z travel executes reliably yet).
- `scripts/validate_prediction.py` — external review's "P2" finding: validates B3's own predicted future margin against what's later physically observed under measured robot-state evolution (`m_phys` vs. `m_phys_observed`, see "Real bugs found and fixed" #17 and #25a for the precise claim), reporting RMSE/MAE (#20) and a per-horizon breakdown (#25b). Now called automatically from `exp3_interaction_force.py`'s own `main()` (#25c), or standalone: `python3 validate_prediction.py /tmp/some_b3_run`. `validate_predictions(bag_dir, dt=0.02)` returns the per-prediction list; `compute_summary(results)` returns `{n, mean_error, mae, rmse, max_abs_error}`; `summarize_by_horizon(results)` returns `{horizon_s: compute_summary_dict}`.

Run any of them with the conda env's own interpreter (plain `python3` on `PATH` resolves to the wrong one on this machine):
```bash
/opt/homebrew/Caskroom/miniconda/base/envs/ros_env/bin/python3 scripts/test_fr3_hybrid_planning.py
```

## Next steps

1. **Goal-execution-fragility: root cause fixed (#33), full `task_success` on `"large"` still not reached — re-verify the plateau before doing anything else with this goal.** A fifth external review found the actual root cause (unused `<position>` actuators in `fr3.xml` silently fighting the real effort controller, capped at exactly each joint's own `actuatorfrcrange` — what every earlier "sharp torque threshold" finding was actually measuring, not real rigid-body physics). Fixed and independently re-verified live: behavior is now qualitatively different (clean, non-oscillating convergence) rather than a stuck/oscillating equilibrium, but `final_pos_error_rad` plateaus around 0.12-0.16 rad (30-200s), not `task_success=True`. The prior six-pass investigation's own final "real physical/safety boundary" conclusion is retracted, but the gravity+Coriolis feedforward controller it produced (`fr3_gravity_ff_controller`) is a real, independent fix NOT invalidated by this — still recommended (opt-in). Next: root-cause the remaining 0.12-0.16 rad plateau (see #33's own three open hypotheses) before treating `"large"` as reliable.
2. **Finish Phase 4c: Exp4** (known-in-advance contact transition) once (1) is resolved — `scripts/exp4_contact_transition.py` and the spring-mode injection mechanism are already built and ready, just blocked on having a working goal.
3. **Phase 4d: flagship reroute + severity sweep + environment-conditioned reroute** (Exp 5/6/7 analogs), reusing Phase 3d's Level 3 + Phase 4b's payload + Phase 4c's force injection together.
4. **Phase 5: run everything, fold real numbers into the paper** — both Phase 4c-fix results are real, repeatedly-reproduced, and ready to use, WITH the corrected framing from #32: Exp2 (B3's certificate activates 4kg earlier than B2's reactive limit — describe as activation-threshold separation, not "prediction gives a 4kg lead", per #32c) and Exp3 (B3 detects `~0.32-0.34s` after local execution begins — NOT "after force onset" — `T_warning≈0.40-0.41s`; B2 never detects; the direct predictor-accuracy number, `validate_prediction.py`'s RMSE, is now `~0.19` N·m after fixing its own timestamp bug, #32a — see "Real bugs found and fixed" #24, #32 for the full derivation). Exp2's own task-completion framing needs updating per #33 above before it goes in the paper, though — and use the LATEST `min_margin` sweep numbers (see #23's own re-verification), not the earlier Phase 4c-fix ones — the goal-execution-fragility mitigation shifted some mid-payload values slightly (the certificate-lead conclusion itself is unaffected, just its wording per #32c).
5. **Optional, not blocking: find a via-point offset that actually recovers margin.** Phase 3d's own verification found 6 tried offsets all made a specific test scenario worse (see Phase 3d history above) — the mechanism is correct, but nobody has yet found a case where Level 3 demonstrably helps. Worth a fresh empirical pass with `B3_DEBUG_HORIZON` if a real experiment needs it, but not worth chasing for its own sake.
6. **Sanity-check `m_safe = 2.0`** at real FR3 scale once there's a concrete experiment to check it against (see "Known environmental gaps").
7. This repo now pushes to `https://github.com/yycao68/fr3-hybrid-planning` (private). The third-party `mujoco_ros2_control` clone's own local-only patch remains local-only (no push access to its upstream, `moveit/mujoco_ros2_control`).
